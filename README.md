This is a script I used to convert my videos from h264 to h265 in order to save space on my NAS.

It will go over every file in a directory, filter out anything other then MP4/MKV files, convert them to h265 (if they aren't already) and delete the source file.
